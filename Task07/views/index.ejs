<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home | GitHub OAuth App</title>

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    body {
      background: linear-gradient(135deg, #89f7fe, #66a6ff);
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      color: #333;
    }

    .container {
      background: #fff;
      border-radius: 16px;
      padding: 2.5rem 3rem;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.15);
      text-align: center;
      max-width: 550px;
      width: 90%;
      animation: fadeIn 1s ease-in-out;
    }

    h1 {
      font-size: 2rem;
      color: #0077b6;
      margin-bottom: 1rem;
    }

    p {
      font-size: 1.1rem;
      margin-bottom: 1.3rem;
      line-height: 1.5;
    }

    a.btn {
      display: inline-block;
      background: #0077b6;
      color: #fff;
      text-decoration: none;
      padding: 0.75rem 1.5rem;
      border-radius: 8px;
      transition: all 0.3s ease;
      font-weight: 500;
    }

    a.btn:hover {
      background: #005f87;
      transform: translateY(-2px);
    }

    a.link {
      display: inline-block;
      text-decoration: none;
      color: #0077b6;
      background: #eaf4fc;
      padding: 0.5rem 1rem;
      border-radius: 6px;
      transition: all 0.3s ease;
      margin: 0.3rem;
    }

    a.link:hover {
      background: #cde6f7;
    }

    .logout {
      background: #e63946;
      color: #fff;
    }

    .logout:hover {
      background: #b71c1c;
      transform: translateY(-2px);
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>

<body>
  <div class="container">
    <h1>👋 Welcome to GitHub OAuth App</h1>

    <% if (!user) { %>
      <p>Welcome — please sign in with GitHub to explore OAuth and API integration.</p>
      <a class="btn" href="/auth/github">Sign in with GitHub</a>
    <% } else { %>
      <p>Signed in as <strong><%= user.username %></strong>.</p>

      <div>
        <a class="link" href="/api/github/profile">View GitHub Profile (Server Call)</a><br>
        <a class="link" href="/api/github/repos">List Repositories (JSON)</a><br>
        <a class="btn logout" href="/auth/logout">Logout</a>
      </div>
    <% } %>
  </div>
</body>
</html>
